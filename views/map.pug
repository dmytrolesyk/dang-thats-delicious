extends layout

block content
  .inner
    h2 #{title}
    .map
      .autocomplete
        input.autocomplete__input(
          type="text"
          name="geolocate"
          placeholder="Search for Stores"
        )
      #map
        p Locading Map...
